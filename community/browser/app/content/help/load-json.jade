article.help
    .container-fluid
        section.row-fluid.summary
            .summary.span3
                h3 LOAD JSON
                p.lead Load data from a JSON file
            .details.span9
                p
                    | The&nbsp;
                    code LOAD JSON
                    |  clause instructs Neo4j to load data from a JSON file
                    | located at the given URL. This is typically used
                    | with&nbsp;
                    code CREATE
                    |  and&nbsp;
                    code MERGE
                    |  to import tabular data into the graph. If you are
                    |  importing a substantially sized data set, you should
                    |  consider using&nbsp;
                    code LOAD JSON
                    |  in combination with&nbsp;
                    code USING PERIODIC COMMIT
                    |  .
                table.table-condensed.table-help
                    tr
                        th Reference:
                        td
                            code
                                a(href='http://docs.neo4j.org/chunked/{{neo4j.version}}/cypher-import.html') LOAD JSON
                            |  manual page;&nbsp;
                            code
                                a(href='http://docs.neo4j.org/chunked/{{neo4j.version}}/cypher-periodic-commit.html') USING PERIODIC COMMIT
                            |  manual page
                    tr
                        th Related:
                        td
                            a(help-topic='CREATE') :help CREATE
                            | &nbsp;
                            a(help-topic='MERGE') :help MERGE
                            | &nbsp;
                            a(help-topic='cypher') :help Cypher
        section.row-fluid.example
            .span9.offset3
                figure
                    pre.code.runnable.
                      LOAD JSON FROM "file://./person.json" AS line
                      MERGE (n:Person {id: toInt(line.id)})
                      SET n.name = line.name
                      RETURN n
                    figcaption Import Person nodes from a JSON file into the graph
